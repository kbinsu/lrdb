services:
  - type: web
    name: loss-ratio-panel-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: panel serve app.py --address 0.0.0.0 --port $PORT --allow-websocket-origin="*"